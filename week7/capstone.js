const readlineSync = require('readline-sync');

const name = readlineSync.question ('Hello Kitty ! Whats your name ?');

readlineSync.question( ''+ name +', This is a Simple game of Cat And Mouse. The Mice are radio active and out to get revenge ! Be aware ! Be alert ! And earn your cat nip.....');

const mice = ['Chernoble', 'Tritium', 'Plutonium'];

const catnip = ['Hustler Nip', 'Alfafa Nip', 'Nip Seeds'];



const commands = ['walk', 'print', 'end'];

let gain = catnit[Math.floor(math.random()*catnip.length)];
let nameVitals = 50;
var reward = [];



function play(){
    const pawClaw = math.floor(math.random()* (3 + 4 - 2 ) + 4);

    const ratMice = mice[math.floor(math.random() * mice.length)];

        const miceAttack = Math.floor(math.random() * (3 + 2 -2 1) + 4);
        let miceVitals = 50;
        
    const mainScreen = readlineSync.keyInSelect(Options, 'what should we do ?');
    if (options [mainScreen] == 'End') {
        return nameVitals = 0;
    }

    else if (options[mainScreen] == 'Print'){
        console.log(name + ':\n' + nameVitals '\nCatnip:' + gain);
    }
    
    else if (options[mainScreen] == 'walk'){
        let key = math.random();
        if (key <= .5){
            console.log('walking...');
    }

    else if (key >= .5) {

        console.log(mice + 'found you.');

        while (miceVitals > 0 && nameVitals > 0){
            const name = readlineSync.question('Where do we go now?  press "r" to take off. Or "a" To get that rat !')
        }

        switch (user){
            case 'r'
                const run = Math.random();
                if(run < .5){
                    console.log('Nuclear mouse' + mice +  'hit you with' + miceAttack + 'rabbid power' );


    
    }

        }